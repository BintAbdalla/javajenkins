pipeline {
    agent any

    environment {
        DOCKER_HUB_USER = "bintabdallah"
        IMAGE_NAME = "javajenkins"
        DEPLOY_PORT = "8080"
    }

    stages {
        stage('Checkout') {
            steps {
                git branch: 'main',
                    url: 'https://github.com/BintAbdalla/javajenkins.git',
                    credentialsId: 'github-credentials-java'
            }
        }

        stage('Check Java & Maven') {
            steps {
                echo "🔍 Vérification des outils Java et Maven"
                sh 'java -version'
                sh 'javac -version'
                sh 'mvn -version'
            }
        }

        stage('Build Maven') {
            steps {
                echo '📦 Compilation et packaging du projet Java avec Maven...'
                sh 'mvn clean package -DskipTests'
            }
        }

        stage('Docker Build & Push') {
            steps {
                script {
                    echo "🐳 Build de l'image Docker..."
                    def image = docker.build("${DOCKER_HUB_USER}/${IMAGE_NAME}:${env.BUILD_NUMBER}")
                    
                    echo "⬆️ Push sur Docker Hub..."
                    docker.withRegistry('https://index.docker.io/v1/', 'docker-hub') {
                        image.push()
                        image.push("latest")
                    }
                }
            }
        }

        stage('Deploy') {
            steps {
                script {
                    echo "🚀 Déploiement du container Docker..."

                    // Arrêter et supprimer le container existant
                    sh """
                        if [ \$(docker ps -aq -f name=${IMAGE_NAME}) ]; then
                            docker stop ${IMAGE_NAME} || true
                            docker rm ${IMAGE_NAME} || true
                        fi
                    """

                    def deployPort = env.DEPLOY_PORT

                    // Vérifier si le port 8080 est occupé
                    def port8080Used = sh(script: "lsof -i:8080 > /dev/null 2>&1", returnStatus: true) == 0
                    if (port8080Used) {
                        def port8081Used = sh(script: "lsof -i:8081 > /dev/null 2>&1", returnStatus: true) == 0
                        deployPort = port8081Used ? "8082" : "8081"
                    }

                    echo "📍 Déploiement sur le port ${deployPort}"

                    sh """
                        docker run -d -p ${deployPort}:8080 --name ${IMAGE_NAME} ${DOCKER_HUB_USER}/${IMAGE_NAME}:latest
                        sleep 5
                        if docker ps | grep ${IMAGE_NAME} > /dev/null; then
                            echo "✅ Container Java démarré avec succès"
                            docker logs ${IMAGE_NAME} --tail 10
                        else
                            echo "❌ Erreur lors du démarrage du container"
                            docker logs ${IMAGE_NAME} || true
                            exit 1
                        fi
                    """

                    // Vérifier la connectivité
                    sh """
                        echo "🔍 Test de connectivité..."
                        sleep 3
                        if curl -f http://localhost:${deployPort} > /dev/null 2>&1; then
                            echo "✅ Application Java répond correctement"
                        else
                            echo "⚠️ L'application ne répond pas encore (peut nécessiter plus de temps)"
                        fi
                    """
                }
            }
        }
    }

    post {
        always {
            echo "🧹 Nettoyage de l'espace de travail..."
            cleanWs()
        }
        success {
            echo '✅ Pipeline exécuté avec succès !'
            echo '🎉 L\'application Java a été déployée correctement'
        }
        failure {
            echo '❌ Pipeline échoué !'
            script {
                echo "🧹 Nettoyage des containers en cas d'échec..."
                sh """
                    if [ \$(docker ps -aq -f name=${IMAGE_NAME}) ]; then
                        docker stop ${IMAGE_NAME} || true
                        docker rm ${IMAGE_NAME} || true
                    fi
                """
            }
        }
    }
}
