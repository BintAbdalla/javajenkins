pipeline {
    agent {
        docker { image 'maven:3.9.3-eclipse-temurin-17' }
    }
    stages {
        stage('Debug') {
            steps {
                sh 'java -version'
                sh 'mvn -version'
                sh 'pwd'
                sh 'ls -la'
            }
        }
    }
}
